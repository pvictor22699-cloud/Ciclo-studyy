{
  "name": "ciclo-study",
  "version": "1.0.0",
  "private": true,
  "description": "Ciclo de estudos com repetição espaçada — motor original (engine.js) sobre Supabase, com API e painel do professor.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:e2e": "node --test \"tests/e2e/*.e2e.js\"",
    "seed": "node scripts/seed-supabase.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {}
}
